server {
 listen 80;
 root /usr/share/nginx/html;
 index index.html;
 location /api/ { proxy_pass http://api:8080; proxy_set_header Host localhost; }
 location / { try_files $uri $uri/ /index.html; }
}

